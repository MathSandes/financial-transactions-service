Instruções rápidas para rodar testes:

1. Garanta que o Postgres esteja rodando (`npm run docker:up`)
2. Exporte a variável de ambiente `DATABASE_URL` se necessário (ou copie .env.example para .env)
3. Rode `npm test`
